# 图书馆座位预约系统开发说明

本文档是 Claude Code 后续开发的唯一主要参考。开发时优先遵守本文档，不要引入本文档之外的复杂架构和额外功能。

## 一、项目目标

实现一个前后端分离的图书馆座位预约系统，用于提高座位利用率，减少占座、空座问题。

系统包含两类角色：

- 普通用户：登录、查看座位、查看座位详情、预约座位、取消预约、签到。
- 管理员：登录、查看所有预约记录、查看座位使用情况、查看统计数据。

## 二、功能模块

### 1. 登录与角色模块

- 用户和管理员共用登录入口。
- 登录后根据角色跳转到不同页面。
- 后端需要区分 `user` 和 `admin` 两种角色。

### 2. 座位查看模块

- 图形化展示座位布局。
- 展示座位实时状态：空闲、已预约、使用中。
- 点击座位后展示座位编号、状态、预约时间等详情。
- 采用redis对作为查看模块进行缓存优化。

座位不保存状态字段，所有实时状态由 `reservation` 动态计算：

```text
using       当前时间存在 checked_in 预约
reserved    当前时间存在 reserved 预约
available   没有当前有效预约
```

### 3. 预约模块

- 用户可选择座位、开始时间、结束时间并提交预约。
- 用户可取消自己的预约。
- 用户可对预约进行签到。

预约规则：

- 开放时间：08:00-21:00。
- 预约范围：当天当前时间开始，至次日 21:00 结束。
- 单次预约不少于 1 小时。
- 不允许预约过去时间。
- 不允许与已有有效预约冲突。
- 同一用户只能预约一个座位，如果还想预约，只能等本次预约时间结束。
- 点击的座位被预约了，可以继续预约后续的时间。

时间冲突判断：

```text
newStart < oldEnd && newEnd > oldStart
```

只和 `reserved`、`checked_in` 状态的预约记录判断冲突。

预约状态固定为：

```text
reserved    已预约
checked_in  已签到
cancelled   已取消
expired     已失效
```

### 4. 管理员统计模块

- 查看所有预约记录。
- 查看座位使用率。
- 查看用户预约次数。
- 查看当前空闲、已预约、使用中座位数量。

## 三、数据库设计

底座只保留系统必需的三张表：

```text
user          用户表：登录账号、密码、角色
seat          座位表：座位编号、位置
reservation   预约表：用户、座位、预约时间段、预约状态
```

字段规则：

- 所有表使用 `id` 作为自增主键。
- 所有表保留 `create_time`、`update_time`。
- `reservation.user_id` 外键关联 `user.id`。
- `reservation.seat_id` 外键关联 `seat.id`。
- 时间冲突和实时座位状态通过业务逻辑和索引辅助判断，不在数据库里写复杂触发器。

核心索引：

```text
user.username 唯一索引
seat.seat_no 唯一索引
seat.location 普通索引
reservation(seat_id, start_time, end_time) 时间冲突查询索引
reservation(user_id, status, end_time) 用户有效预约查询索引
```

初始化脚本位置：

```text
src/main/resources/schema.sql
```

该脚本会删除旧表并重建基础表，只用于开发和课程演示环境。

## 四、架构规则

使用最简单的分层架构，不使用复杂设计模式。

后端调用流程固定为：

```text
Controller -> Service -> Mapper -> Database
```

后端目录建议：

```text
controller/   接收请求，返回统一响应
service/      业务逻辑
mapper/       MyBatis 数据库访问
entity/       数据库实体
dto/          请求和响应对象
common/       统一返回、错误码、业务异常
config/       配置类
```

各层职责：

- Controller：接收请求、基础参数校验、调用 Service、返回统一响应。
- Service：处理业务规则，例如登录校验、预约时间校验、预约冲突判断。
- Mapper：只负责数据库增删改查，不写业务判断。
- Entity：对应数据库表。
- DTO：用于接口请求和响应，不直接把 Entity 暴露给前端。

禁止事项：

- 不要在 Controller 写复杂业务逻辑。
- 不要在 Mapper 写业务判断。
- 不要引入工厂、策略、事件总线、消息队列等复杂设计。
- 不要扩展项目计划之外的大功能。
- 不要把所有代码堆在一个类里。

允许的简单公共封装：

- `ApiResponse<T>`：统一接口返回。
- `ErrorCode`：统一错误码。
- `BusinessException`：业务异常。
- `GlobalExceptionHandler`：全局异常处理。

## 五、接口规则

接口路径使用资源名，统一加 `/api` 前缀。

建议接口：

```text
POST   /api/login
GET    /api/seats
GET    /api/seats/{id}
POST   /api/reservations
GET    /api/reservations/my
DELETE /api/reservations/{id}
POST   /api/reservations/{id}/check-in
GET    /api/admin/reservations
GET    /api/admin/statistics
```

请求参数规则：

- 查询接口使用 query 参数，例如 `GET /api/seats?location=三楼`。
- 新增、修改接口使用 JSON 请求体。
- 时间格式统一为 `yyyy-MM-dd HH:mm:ss`。

创建预约请求示例：

```json
{
  "seatId": 1,
  "startTime": "2026-05-18 14:00:00",
  "endTime": "2026-05-18 16:00:00"
}
```

## 六、统一返回格式

所有后端接口统一返回：

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

规则：

- `code = 0` 表示成功。
- `code != 0` 表示业务失败。
- `message` 返回用户可理解的信息。
- `data` 没有数据时返回 `null`。
- 前端优先根据 `code` 判断业务是否成功。

成功示例：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "seatNo": "A01"
  }
}
```

失败示例：

```json
{
  "code": 2001,
  "message": "预约时间与已有预约冲突",
  "data": null
}
```

## 七、错误码规则

错误码按模块分段：

```text
0           成功
1000-1999   用户与登录
2000-2999   预约业务
3000-3999   座位业务
4000-4999   管理员权限
9000-9999   系统通用错误
```

固定错误码：

```text
0     success
1001  用户名或密码错误
1002  用户未登录
1003  用户不存在
2001  预约时间与已有预约冲突
2002  预约开始时间不能早于当前时间
2003  单次预约时间不能少于1小时
2004  预约时间不在开放时间内
2005  预约记录不存在
2006  只能取消自己的预约
3001  座位不存在
3002  座位当前不可预约
4001  无管理员权限
9001  请求参数错误
9002  系统异常
```

约束：

- 不允许把后端异常堆栈返回给前端。
- 同一错误场景使用固定错误码。
- Controller 不直接拼错误码，统一通过 `ApiResponse`、`ErrorCode` 或 `BusinessException` 返回。

## 八、命名规则

Java 命名：

```text
类名：大驼峰，例如 ReservationService、SeatController
方法和变量：小驼峰，例如 createReservation、seatId
常量：全大写下划线，例如 MIN_RESERVATION_HOURS
```

后端类命名：

```text
XxxController
XxxService
XxxMapper
XxxRequest
XxxResponse
```

数据库命名：

```text
表名和字段名使用小写下划线，例如 seat_no、start_time、create_time
```

接口字段命名：

```text
JSON 字段使用小驼峰，例如 seatId、seatNo、startTime
```

Git 分支：

```text
main                  稳定主分支
feature/login         登录模块
feature/seat          座位模块
feature/reservation   预约模块
feature/admin         管理员模块
fix/xxx               问题修复
```

## 九、开发检查清单

每次生成或修改代码前后检查：

- 是否只实现本文档范围内的功能。
- 是否使用最简单分层架构。
- 业务规则是否放在 Service 层。
- 接口是否使用统一返回格式。
- 错误码是否来自本文档。
- 命名是否统一。
- 预约冲突判断是否正确。
- 是否没有引入不必要的复杂设计。

## 十、环境变量配置

数据库连接通过环境变量配置，不要硬编码到配置文件中：

```text
DB_URL       数据库连接地址，默认 jdbc:mysql://localhost:3306/agentforge?...
DB_USERNAME  数据库用户名，默认 root
DB_PASSWORD  数据库密码，默认空
```

本地开发可在 IDE 或 `.env` 中设置，部署时通过系统环境变量传入。
