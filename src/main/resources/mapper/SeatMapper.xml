<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.seat.mapper.SeatMapper">

    <select id="findAll" resultType="Seat">
        SELECT id, seat_no, location, create_time, update_time
        FROM seat
    </select>

    <select id="findByLocation" resultType="Seat">
        SELECT id, seat_no, location, create_time, update_time
        FROM seat
        WHERE location = #{location}
    </select>

    <select id="findById" resultType="Seat">
        SELECT id, seat_no, location, create_time, update_time
        FROM seat
        WHERE id = #{id}
    </select>
</mapper>
